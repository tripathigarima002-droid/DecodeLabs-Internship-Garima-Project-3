The project involves the coding through SQL, the foremost query is the SELECT statement which also uses the wild card character '*'.
The SELECT DISTINCT statement has also been involved in the query. 
Other than all these the ORDER BY and the GROUP BY statement has been used to fetch the results from the Data sheet specifically for OrderID, Date, UnitPrice.
The Aggregate functions COUNT, SUM, AVG are applied to the Unit Price and Total Price.
